% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_logic.R
\name{calc_events}
\alias{calc_events}
\alias{calc_skipped_exons}
\alias{calc_included_exons}
\alias{calc_mx_exons}
\alias{calc_retained_introns}
\alias{calc_alt_ss}
\alias{calc_a5ss}
\alias{calc_a3ss}
\alias{calc_all_events}
\title{Calculate splice events from a GRanges object}
\usage{
calc_skipped_exons(gr, type = c("boundary", "over", "in"), inverse = FALSE)

calc_included_exons(gr, type = c("boundary", "over", "in"))

calc_mx_exons(gr, type = c("boundary", "in", "over"))

calc_retained_introns(gr)

calc_alt_ss(gr, by_start = TRUE)

calc_a5ss(gr)

calc_a3ss(gr)

calc_all_events(gr, type = c("boundary", "over", "in"), verbose = TRUE)
}
\arguments{
\item{gr}{A GRanges object with exon annotations, including 'tx_id', 'exon',
and 'coef_col' metadata columns and preprocessed with preprocess().}

\item{type}{The type of overlap to consider when
identifying events.}

\item{inverse}{If TRUE, identifies included exons
instead of skipped exons.}

\item{by_start}{If TRUE, detects a5ss (same exon start, different end).
If FALSE, detects a3ss (same end, different start).}

\item{verbose}{If TRUE, prints progress messages. Default TRUE.}
}
\value{
\code{calc_skipped_exons()}: A GRanges object with
an additional 'event' metadata column indicating
skipped exons.

\code{calc_included_exons()}: A GRanges object with an additional 'event'
metadata column indicating included exons.

\code{calc_mx_exons()}: A GRanges object with an additional 'event'
metadata column indicating mutually exclusive exons.

\code{calc_retained_introns()}: A GRanges object with an additional
'event' metadata column indicating retained introns.

\code{calc_alt_ss()}: A GRanges object with annotated events for
alternative splice sites.

\code{calc_a5ss()}: A GRanges object with an additional 'event' metadata
column indicating a5ss events.

\code{calc_a3ss()}: A GRanges object with an additional 'event' metadata
column indicating a3ss events.

\code{calc_all_events()}: A GRanges object combining all detected events,
with an 'event' metadata column indicating the event type.
}
\description{
Functions to detect different types of alternative splicing
events from preprocessed GRanges exon data.
}
\examples{

# make some mock data and run the function
gr <- create_mock_data(n_genes = 2, n_tx = 4, n_exons = 4) |>
  preprocess(coef_col = "coefs") |>
  generate_skipped_exons(1)

# this should find the skipped exon events we generated
calc_skipped_exons(gr, type = "boundary")

calc_included_exons(gr, type = "boundary")


# detect mutually exclusive exons
gr_mx <- create_mock_data(
  n_genes = 2, n_tx = 4, n_exons = 4
) |>
  preprocess(coef_col = "coefs") |>
  generate_mx(1)
calc_mx_exons(gr_mx, type = "boundary")


# detect retained introns
gr_ri <- create_mock_data(
  n_genes = 2, n_tx = 4, n_exons = 4
) |>
  preprocess(coef_col = "coefs") |>
  generate_retained_introns(1)
calc_retained_introns(gr_ri)


# detect alternative 5' splice sites
gr_a5 <- create_mock_data(
  n_genes = 2, n_tx = 4, n_exons = 4
) |>
  preprocess(coef_col = "coefs") |>
  generate_a5ss(1)
calc_a5ss(gr_a5)


# detect alternative 3' splice sites
gr_a3 <- create_mock_data(
  n_genes = 2, n_tx = 4, n_exons = 4
) |>
  preprocess(coef_col = "coefs") |>
  generate_a3ss(1)
calc_a3ss(gr_a3)


# detect all event types at once
gr_all <- create_mock_data(
  n_genes = 2, n_tx = 4, n_exons = 4
) |>
  preprocess(coef_col = "coefs") |>
  generate_skipped_exons(1)
calc_all_events(gr_all, type = "boundary", verbose = FALSE)

}
