# Re-rank exons in a GRanges object

Re-rank exons in a GRanges object

## Usage

``` r
rerank_exons(gr)
```

## Arguments

- gr:

  A GRanges object with metadata columns: 'exon_rank'

## Value

A GRanges object with re-ranked exons
